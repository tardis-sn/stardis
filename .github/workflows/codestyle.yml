name: codestyle

on:
  push:
    branches:
      - "*"
  pull_request:
    branches:
      - "*"

jobs:
  ruff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run Ruff
        uses: astral-sh/ruff-action@v3
        with:
          args: check stardis
